# JWT_SECRET=change-me-to-a-random-64-char-hex-string

# # --- DB / ports / runtime (added for Phase 1 runtime verification) ---
# POSTGRES_USER=transcendence_admin
# POSTGRES_PASSWORD=your-db-password
# POSTGRES_DB=transcendence_db
# DATABASE_URL=postgresql://user:password@database:5432/dbname?schema=public
# BACKEND_PORT=3000
# FRONTEND_PORT=5173
# NODE_ENV=development

# # --- HashiCorp Vault (Phase 2) ---
# VAULT_ROOT_TOKEN=change-me-vault-root-token
# # Least-privilege token the backend uses (read-only on secret/backend/*); created by vault-init.
# VAULT_BACKEND_TOKEN=change-me-vault-backend-token

# # --- Nginx + ModSecurity WAF (Phase 3) ---
# WAF_PORT=8080
# WAF_SSL_PORT=8443


# --- Data base PostgreSQL ---
POSTGRES_USER=transcendence_admin
POSTGRES_PASSWORD=super_secret_password
POSTGRES_DB=transcendence_db

# A URL que o Prisma (no backend) vai usar para se ligar à base de dados
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}?schema=public

# --- NestJS port connection ---
BACKEND_PORT=3000

# --- React connection ---
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:3000
NODE_ENV=development
# NODE_ENV=production


JWT_SECRET=01f222dbf4cff3455c3919f2989384477604d025e9754c9b819b176d6ed51f72
VAULT_ROOT_TOKEN=868c8c53cf9d00efad7329aab1d2bffe594c369a4547dac3a98b9a66c50cd300
VAULT_BACKEND_TOKEN=fc31cc45bcb9ee86c009468dfd8a7d783f167d6f8646c918c4d6e793867d11c6
WAF_PORT=8080
WAF_SSL_PORT=8443

